import type { SiteConfig } from "@/types/site";

export const siteConfig: SiteConfig = {
  name: "Site Core",
  description: "Base reutilizável para sites comerciais.",
  locale: "pt-BR",
  contact: {
    whatsapp: "",
  },
  social: {},
};
